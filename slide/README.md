# Slides locales (TypeScript)

## Build
```bash
cd slide
npm install
npm run build
```

## Lancer en localhost
```bash
cd slide
python3 -m http.server 4173
```

Puis ouvrir:
- http://127.0.0.1:4173

## Contrôles
- Boutons `Précédent` / `Suivant`
- Flèches clavier `←` `→`
- `PageUp` / `PageDown`
